#  java-db-controller-mysql
 A simple database controller for mysql


## Run
Running this application requires a running lampp server, with a database called `test` and table `PERSON`, the application should be able to setup the data accordingly given these.

